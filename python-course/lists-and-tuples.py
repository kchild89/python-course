# list = can change going to use about 70% to 80% in the real world

# list + functions + methods
my_list = [1, 2, 3, 4.5, "word"]
# print(len(my_list))
# my_list.reverse()
# my_list.append("append")  # add to the end of a list or array
# print(my_list)

# tuple = cant change a tuple

my_tuple = (1, 2, 3, 1.45, "Word", [7, 8, 9])
# print(my_tuple)

# how to pick elements from a tuple or a list -> Indexing or slicing
# print(my_list[0])
# print(my_tuple[5][0])  # first brackett targets the order in the lise second bracket targets the second list within the list
# print(my_tuple[-1])

# execise
# get the word / string 'hello' :)'
exercise_list = ["firse entry", [123, 456, [0, "Hello :)"]], "bye"]
solution_var = exercise_list[1][2][1]
print(solution_var)
